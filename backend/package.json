{
  "name": "fayolla-backend",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate": "wrangler d1 execute fayolla-db --file=./migrations/0001_initial.sql && wrangler d1 execute fayolla-db --file=./migrations/0011_temptation_bundling.sql && wrangler d1 execute fayolla-db --file=./migrations/0012_notification_events.sql",
    "db:migrate:remote": "wrangler d1 execute fayolla-db --remote --file=./migrations/0001_initial.sql && wrangler d1 execute fayolla-db --remote --file=./migrations/0011_temptation_bundling.sql && wrangler d1 execute fayolla-db --remote --file=./migrations/0012_notification_events.sql"
  },
  "dependencies": {
    "@block65/webcrypto-web-push": "^1.0.2",
    "hono": "^4.4.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240725.0",
    "typescript": "^5.5.0",
    "wrangler": "^4.103.0"
  }
}
